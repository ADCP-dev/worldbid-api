/**
 * Test stub for Nuxt's '#imports' virtual module — used only by unit tests
 * that import composables relying on auto-imported stores.
 */
import { useAuthStore } from '../../modules/base/auth/stores/auth.store';

export { useAuthStore };
